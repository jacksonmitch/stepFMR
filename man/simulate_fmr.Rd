% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_fmr.R
\name{simulate_fmr}
\alias{simulate_fmr}
\title{Simulate Data from a Finite Mixture Regression}
\usage{
simulate_fmr(
  n,
  betas,
  pi = NULL,
  sigma = 1,
  family = c("gaussian", "poisson", "binomial"),
  size = 1,
  seed = NULL
)
}
\arguments{
\item{n}{A positive integer, the number of observations to simulate.}

\item{betas}{A numeric matrix of true coefficients, one row per mixture
component (or a list of numeric vectors, one per component, which
will be row-bound into a matrix). If column names are supplied, one
of them must be \code{"(Intercept)"}; if omitted, the first column is
assumed to be the intercept and named accordingly.}

\item{pi}{An optional numeric vector of mixing proportions, one per
component (normalized internally). Defaults to equal proportions.}

\item{sigma}{A numeric vector of per-component residual standard
deviations (Gaussian family only). Recycled to the number of
components if length 1. Default is 1.}

\item{family}{A character string, one of \code{"gaussian"}, \code{"poisson"}, or
\code{"binomial"}.}

\item{size}{For \code{family = "binomial"}, the number of trials per
observation (recycled if length 1). Default is 1.}

\item{seed}{An optional integer passed to \code{set.seed()} for reproducible
simulation.}
}
\value{
A list with elements: \code{data} (a data.frame with the response
\code{y}, the covariates, and — for \code{family = "binomial"} — a \code{size} trial
count column), \code{formula} (ready to pass to \code{stepFMR()}; a
\code{cbind(y, size - y) ~ .} form for binomial), and \code{true_group} (the
simulated integer group labels, for use with \code{match_groups()}).
}
\description{
Simulates covariates, group membership, and a response from a specified
finite mixture regression model. Column/row names on \code{betas} are
optional. If omitted, columns become \verb{"(Intercept)", "x1", "x2", ...}
and rows become \verb{"g1", "g2", ...}.
}
\examples{
betas <- rbind(
  c(-2, -3, 1),
  c(2, 3, 1)
)
sim <- simulate_fmr(n = 300, betas = betas, seed = 1)
fit <- stepFMR(sim$formula, sim$data, G_max = 3)
fit_aligned <- match_groups(fit, sim$true_group)
}
