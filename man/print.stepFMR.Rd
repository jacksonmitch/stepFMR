% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s3-methods.R
\name{print.stepFMR}
\alias{print.stepFMR}
\title{Print a stepFMR Result}
\usage{
\method{print}{stepFMR}(x, ...)
}
\arguments{
\item{x}{An object of class \code{stepFMR}, as returned by
\code{\link{stepFMR}}.}

\item{...}{Currently unused.}
}
\value{
\code{x}, invisibly.
}
\description{
Prints a top-level summary of a \code{\link{stepFMR}} fit: the family, the
candidate \code{G} values, and which procedure(s) were run, followed by the
variable-selection result (if run), the effect-determination result (if
run), and the best fit.
}
\examples{
pi <- c(0.2, 0.3, 0.5)
sigma <- c(1, 0.5, 2)
betas <- rbind(
  c(-4, -3, 1.5, 2, 0),
  c(0, 1, 1.5, -1, 0),
  c(4, 3, 1.5, 0.5, 0)
)
sim <- simulate_fmr(n = 500, betas = betas, pi = pi, sigma = sigma, seed = 1)
fit <- stepFMR(sim$formula, sim$data, G_max = 3)
print(fit)
}
