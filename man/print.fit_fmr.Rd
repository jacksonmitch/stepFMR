% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s3-methods.R
\name{print.fit_fmr}
\alias{print.fit_fmr}
\title{Print a Fitted Finite Mixture Regression Model}
\usage{
\method{print}{fit_fmr}(x, ...)
}
\arguments{
\item{x}{An object of class \code{fit_fmr}. This is produced internally
while fitting each candidate number of components, and surfaces to
users as the \code{best_fit} component of the list returned by
\code{\link{stepFMR}} (always present, regardless of \code{procedure}; see
\code{stepFMR}'s \strong{Value} section).}

\item{...}{Currently unused.}
}
\value{
\code{x}, invisibly.
}
\description{
Prints the number of components \code{G} and, for each parameter block
present on the fit, the estimated mixing proportions, component standard
deviations, heterogeneous coefficients, and homogeneous coefficients.
}
\examples{
pi <- c(0.2, 0.3, 0.5)
sigma <- c(1, 0.5, 2)
betas <- rbind(
  c(-4, -3, 1.5, 2, 0),
  c(0, 1, 1.5, -1, 0),
  c(4, 3, 1.5, 0.5, 0)
)
sim <- simulate_fmr(n = 500, betas = betas, pi = pi, sigma = sigma, seed = 1)
fit <- stepFMR(sim$formula, sim$data, G_max = 3)
# fit$best_fit is always populated, at the BIC-optimal G
print(fit$best_fit)
}
\seealso{
\code{\link{stepFMR}}
}
